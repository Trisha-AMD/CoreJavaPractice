package OOPs;

interface Rectangle {
	public void area(int a, int b);

}

//difference between abstract class and interface
/** Abstract class has both abstract method and normal methods, 
 * but in interface can have only method without definition.
 * In interface , everything is implicitly abstract
 * Object for abstract class can be created using a inherited class name**/
